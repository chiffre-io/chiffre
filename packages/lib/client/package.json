{
  "name": "@chiffre/client",
  "version": "0.0.1",
  "main": "dist/index.js",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "clean": "rm -rf ./coverage ./dist ./node_modules",
    "build": "tsc",
    "e2e:setup": "../../../scripts/e2e-start.sh",
    "e2e:teardown": "../../../scripts/e2e-stop.sh",
    "e2e:test": "jest e2e --runInBand --verbose --coverage",
    "ci": "run-s e2e:setup e2e:test e2e:teardown build"
  },
  "dependencies": {
    "@47ng/cloak": "^0.15.0",
    "@chiffre/api-types": "^0.0.1",
    "@chiffre/crypto-base": "^0.0.1",
    "@chiffre/crypto-client": "^0.0.1",
    "axios": "^0.19.1",
    "session-keystore": "^0.6.0"
  },
  "devDependencies": {
    "@chiffre/api": "^0.0.1"
  },
  "jest": {
    "preset": "ts-jest/presets/js-with-ts",
    "testEnvironment": "node"
  }
}
