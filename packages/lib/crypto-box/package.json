{
  "name": "@chiffre/crypto-box",
  "version": "0.0.2",
  "main": "dist/index.js",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "clean": "rm -rf ./dist ./node_modules",
    "build": "tsc",
    "test": "jest --verbose --logHeapUsage --colors"
  },
  "dependencies": {
    "@47ng/codec": "^0.4.0",
    "tweetnacl": "^1.0.1"
  },
  "jest": {
    "preset": "ts-jest/presets/js-with-ts",
    "testEnvironment": "node"
  }
}
