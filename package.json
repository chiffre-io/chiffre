{
  "private": true,
  "license": "UNLICENSED",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "ci": "run-s test:unit",
    "test:unit": "yarn workspace @chiffre/crypto run test",
    "test:e2e": "run-s dc:build dc:test dc:down",
    "dc:build": "docker-compose build",
    "dc:test": "docker-compose run test-runner",
    "dc:down": "docker-compose down"
  },
  "devDependencies": {
    "jest": "^24.9.0",
    "npm-run-all": "^4.1.5",
    "ts-jest": "^24.2.0",
    "ts-node": "^8.5.4",
    "tsconfig-paths": "^3.9.0",
    "typescript": "^3.7.4"
  }
}
