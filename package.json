{
  "private": true,
  "license": "UNLICENSED",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "yarn wsrun -m clean && rm -rf ./node_modules",
    "build": "yarn wsrun -mt build",
    "ci": "yarn wsrun -ms ci"
  },
  "devDependencies": {
    "jest": "^24.9.0",
    "jest-extended": "^0.11.2",
    "npm-run-all": "^4.1.5",
    "ts-jest": "^24.2.0",
    "ts-node": "^8.6.2",
    "tsconfig-paths": "^3.9.0",
    "typescript": "^3.8.0-dev.20200118",
    "wsrun": "^5.2.0"
  }
}
